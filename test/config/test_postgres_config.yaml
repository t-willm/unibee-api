env: daily

server:
  domainPath: "http://api.unibee.top"

database:
  default:
    link:    "postgres:unibee:changeme@tcp(127.0.0.1:5432)/root"

# Redis Stage
redis:
  default:
    address: 127.0.0.1:6379
    pass: changeme