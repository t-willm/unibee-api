env: daily

server:
  domainPath: "http://api.unibee.top"

database:
  default:
    link:    "mysql:unibee:changeme@tcp(127.0.0.1:3306)/unibee?loc=UTC&parseTime=false"

# Redis Stage
redis:
  default:
    address:  127.0.0.1:6379
    pass: changeme